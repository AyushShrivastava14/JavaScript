let age = 18;

if(age === 18) console.log(`Adult (Strict)`);

if(age == 18) console.log(`Adult (Loose)`);

age = '18';

if(age === 18) console.log(`Adult (Strict)`);

if(age == 18) console.log(`Adult (Loose)`);


// Strict equality operator ===
// Loose equality operator ==

// prompt() use
const n = prompt("Enter your favourite number:-");
console.log(n);